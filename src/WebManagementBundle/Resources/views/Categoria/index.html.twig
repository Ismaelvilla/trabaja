{% extends "base.html.twig" %}
{% block body %}
    <div class=" border d-flex align-items-center p-3 my-3 bg-purple rounded shadow-sm">
        <a href="{{ path('categorias_add') }}">añadir nueva</a>
        <div class="lh-100">
            <h6 class="mb-0 text-white lh-100">Bootstrap</h6>
            <small>Since 2011</small>
        </div>
    </div>
    <div class="my-3 p-3 bg-white rounded shadow-sm">
        <h6 class="border-bottom border-gray pb-2 mb-0">Lista de Empresas</h6>
        <table class="table table-striped table-hover">
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Fecha</th>
                <th>Editar</th>
            </tr>
            {% for categoria in categorias %}
                <tr>
                    <td>{{ categoria.id }}</td>
                    <td>{{ categoria.nombre }}</td>
                    <td>{{ categoria.fecha|date('d/m/Y H:i') }}</td>
                    <td><a href="{{ path('categorias_details',{'id':categoria.id}) }}">Editar</a></td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}